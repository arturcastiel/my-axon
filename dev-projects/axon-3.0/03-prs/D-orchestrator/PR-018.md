# PR-018 — Menu + output-layer UX

**Phase:** D — Orchestrator + UX
**Complexity:** M
**Status:** not-started
**Depends on:** PR-012, PR-015, PR-017

## Carryover from Phase A/B execution

- **Menu hint sweep**: PR-008 already updated `menu.md` to point at `workspace-backup push` (was `save-workspace`). PR-018's menu work should continue from that state — don't re-introduce save-workspace references.
- **Surface the meta tools**: PR-007 made `compile` the canonical entry. The "Self-improvement panel" should highlight `compile rank` and `compile auto-compile` (not the legacy `compile-suggest` names).
- **Drift badge** (from PR-012): the badge already wires into output-layer at PR-012; this PR just adds menu visibility on top.

## Why

Without visibility, autonomy feels like loss of control. The panel makes "what is AXON doing for me" answerable in one glance.

## Affected files

| File | Change |
|---|---|
| `workspace/programs/menu.md` | New "Self-improvement" panel + drift badge |
| `workspace/programs/compiled/menu.cmp.md` | Recompile after menu update |
| `axon/OUTPUT-LAYER.md` | Drift score line item (dev-mode required) |
| `tests/test_programs_md.py` | Menu rendering smoke test |

## Implementation steps

1. **Menu — Self-improvement panel** (added to OS STATE area):
```
  SELF-IMPROVEMENT
  ─────────────────────────────────────────────
  Drift         ●●●●○ stable                          ← from PR-012
  Cron          5 jobs · last run: igap-report 2h ago ← from PR-010+11
  Pending       2 auto-actions queued                  ← from PR-017
  Audit         3 entries since last review · auto-actions
  Auto-mode     OFF (run: kv-store set L:auto-improve true)
```

2. **Drift badge** — OS STATE panel (existing area) gets a Drift line; logic from PR-012's gate.

3. **Output-layer drift** — `axon/OUTPUT-LAYER.md` (dev-mode):
   - Footer compact: `[drift: stable|drifting|diverged]` inline.
   - Footer full: separate row with score + recommended action.

4. **Surface meta tools on menu** — add to QUALITY area:
```
  axon-audit                — full structural + usefulness audit
  compile-optimizer status  — compilation coverage report
  axon-docs-gen             — regenerate AXON-DOCS.md
  auto-actions              — review recent auto-edits          ← from PR-015
```

5. **Recompile menu** — after .md edit, `compile optimize --program menu`.

6. **Tests**:
   - `test_menu_renders_self_improvement_panel` — boot, render menu, grep for "SELF-IMPROVEMENT"
   - `test_drift_badge_shown_when_not_stable` — synthesize drifting state, render menu, grep for "drift"
   - `test_meta_tools_surfaced` — grep for "axon-audit" in menu output

## Test strategy

- Pytest covers menu rendering presence.
- Smoke: real boot, eyeball the panel.

## Rollback plan

Revert menu.md + OUTPUT-LAYER.md changes. Recompile menu.

## Acceptance criteria

- ✓ Menu shows Self-improvement panel
- ✓ Drift badge visible when not stable
- ✓ Meta tools (axon-audit, compile-optimizer, auto-actions) surfaced
- ✓ Output layer footer shows drift
- ✓ Tests pass
- ✓ Health 100, lint clean

## Risks

- Menu becomes long. Mitigation: panel is conditional (only show drift if not stable; only show pending if >0).
- OUTPUT-LAYER.md change requires dev-mode. Standard write-gate workflow.

## Out of scope

- Interactive menu (e.g., arrow keys). Stays text-based.
- Custom themes / colors.
