# PR-004 — Backup: retarget `workspace-backup` + `save-workspace` to `my-axon/`
project: axon-os
branch:  my-axon-backup
status:  not-started
date:    2026-05-07
depends: PR-003

---

## Files changed

- `workspace/programs/workspace-backup.md`
- `workspace/programs/save-workspace.md`

---

## `workspace-backup.md` — key changes

1. **Target path**: change all `W:ws-path` / `"workspace"` refs → `W:myaxon-path`
2. **local/ keys**:
   - `ws-backup-url.md` → `myaxon-backup-url.md`
   - `ws-backup-enabled.md` → `myaxon-backup-enabled.md`
   - `ws-backup-status.md` → `myaxon-backup-status.md`
3. **local-path**: `RETRIEVE(W:myaxon-local)` (was `W:ws-local`)
4. **git-dir check**: `FILE-EXISTS("{W:myaxon-path}/.git")` (was `ws-path`)
5. **Push scope**: only `my-axon/` — never `workspace/`
6. **Setup description**: update to say "your private my-axon repo"
7. **Status output**: show `W:myaxon-path` as the backup target

---

## `save-workspace.md` — key changes

1. All path refs: `W:ws-path` → `W:myaxon-path`
2. local-path: `W:myaxon-local`
3. Commit target: `my-axon/` repo
4. Description updated: "save my-axon data"

---

## KERNEL-SLIM boot auto-push block (also updated in PR-001)

```
local-path ← RETRIEVE(W:myaxon-local) | "{W:myaxon-path}/memory/local/"
backup-enabled ← READ("{local-path}myaxon-backup-enabled.md") | ∅
IF backup-enabled ≡ "true" →
  EXEC(workspace-backup push)
```
