# igap log: 2026-05-05

## [fallback-exec] T:session-end
context:  no-active-program | session turn (post-compaction)
missing:  explicit step for 'run code-dev-explain program' — agent executed the program logic directly instead of dispatching through AXON
action:   user requested `code-dev-explain` to generate `pr01-explained.md`; agent inlined the algorithm and wrote the file directly outside AXON, bypassing program lookup and dispatch entirely
expected: (1) search workspace/programs/ for `code-dev-explain.md`, (2) run via AXON program dispatch, (3) if not found → QUERY(user) before proceeding with any fallback
suggestion: Add rule to KERNEL-SLIM.md §PROGRAM DISPATCH: "IF user names a program → LOOKUP(workspace/programs/{name}.md) → RUN via dispatch → if not found QUERY(user); NEVER execute program logic inline outside AXON"
